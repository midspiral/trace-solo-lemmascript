# Where to sync recorded traces. Leave unset to run fully local-first
# (IndexedDB + manual NDJSON export). Set to your deployed Worker URL to
# aggregate a central corpus. The game plays identically either way.
VITE_TRACE_ENDPOINT=
